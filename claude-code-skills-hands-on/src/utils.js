function greet(name) {
  return `Hello, ${name}!`;
}

function add(a, b) {
  return a + b;
}

// 2つの数値を掛け算して結果を返す
function multiply(a, b) {
  return a * b;
}

module.exports = { greet, add, multiply };
