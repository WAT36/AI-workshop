// 名前を受け取り、挨拶文字列を返す
function greet(name) {
  return `Hello, ${name}!`;
}

// 2つの数値を足し算して結果を返す
function add(a, b) {
  return a + b;
}

// 2つの数値を掛け算して結果を返す
function multiply(a, b) {
  return a * b;
}

module.exports = { greet, add, multiply };
